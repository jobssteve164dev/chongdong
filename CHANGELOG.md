# 变更记录

## 1.0.0 - 2026-08-27

- 连接状态改为由真实核心运行状态驱动，失败路径回滚系统代理。
- 接通静态链入口，动态链不再使用空链或静态回退冒充成功。
- 泄露检查区分“通过、失败、未验证”，移除常量和配置开关生成的假通过。
- IPC 改为显式白名单，系统代理限制到回环地址，移除命令拼接和批量进程终止。
- TLS 默认验证证书；代理核心和数据库下载固定版本并校验 SHA-256。
- 修复 TypeScript、ESLint、Jest 配置，升级受影响依赖并恢复构建门禁。
