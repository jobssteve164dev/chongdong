# 隐私与观测边界

虫洞在本机保存配置、订阅、规则、运行状态和必要日志。代理节点凭据、订阅地址和完整核心配置不应写入普通日志。

出口 IP 检测会通过用户明确选择的本地 SOCKS 入口访问外部 IP 服务。入口不可用时检测失败，不会回退直连来生成结果。

DNS、WebRTC、TLS 指纹、HTTP 头、时序、MAC、时区和语言等项目，只有取得真实运行时或外部观测证据时才可显示“通过”。仅检查到防护开关已启用的项目显示为“未验证”。

清除本地配置或测试数据属于用户主动操作。发布环境还应根据组织的数据保留与审计政策配置日志周期。
