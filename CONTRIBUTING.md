# 贡献说明

虫洞计划服务于堡垒计划内部的七层网络信任目标。改动前请先确认用户路径与信任边界，不要把它改造成面向外部市场的通用代理客户端。

提交前运行：

```bash
npm ci
npm run type-check
npm run lint
npm test
npm run build
npm audit --omit=dev
```

涉及连接状态、泄露检测或安全结论的改动，必须附带可复现的观测证据。配置开关、函数正常返回、模拟值和默认常量都不能作为“已连接”或“已通过”的依据。

涉及代理核心版本时，同时更新固定下载地址、平台产物名和 SHA-256 清单。禁止关闭 TLS 校验来绕过下载或连接问题。
