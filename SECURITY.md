# 安全策略

本仓库用于内部网络信任机制。发现安全问题时，请通过仓库的私有安全报告渠道提交，不要在公开 Issue 中附带节点凭据、订阅地址、PSK、完整代理配置、内部域名或真实出口 IP。

报告至少包含受影响版本、复现路径、影响边界和去敏后的证据。以下问题优先处理：

- 未经授权的 IPC 或系统命令执行；
- 连接状态、防护状态或泄露结论可被伪造；
- 凭据进入日志、错误报告或界面；
- 核心下载未校验、校验绕过或压缩包路径穿越；
- 流量绕过已选择的链路或在失败时静默回退直连。

当前支持范围以 `package.json` 和固定核心清单为准。操作系统签名证书与发布密钥不存放在仓库中。
